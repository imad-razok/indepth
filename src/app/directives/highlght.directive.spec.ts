import { HighlghtDirective } from './highlght.directive';

describe('HighlghtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlghtDirective();
    expect(directive).toBeTruthy();
  });
});
